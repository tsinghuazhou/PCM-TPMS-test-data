# 2026-08-15

## GitHub 同步
- 首次为仓库配置远程 `origin` = https://github.com/tsinghuazhou/PCM-TPMS-test-data
- 提交全部未跟踪/修改文件(212 个):实验数据、分析脚本、报告、论文图件、.openscience 日志
- 远程只有初始提交(README 占位 + MPL-2.0 LICENSE),本地历史与其无关
  - 用 `merge origin/main --allow-unrelated-histories -X ours` 保留本地 README、引入 LICENSE
- 本地分支 `master` 重命名为 `main` 后推送成功
- 教训:本机未安装 `gh`;推送等待 Git Credential Manager 弹窗授权,需把命令 timeout 调大

## 修复 GitHub 乱码(commit 5bdbcfa)
- 问题定位:110 个 `.openscience/logs/*.txt` 会话日志含 U+FFFD 替换符与残留 GBK 字节(双重编码损坏,不可恢复);`tpms_iwp20w_20260809_201218.csv` 为 GBK 编码;`output/paper/*.log` 编译日志编码混杂
- 处理:
  - `.openscience/logs/` 与 `output/paper/*.log` 移出版本库并加入 `.gitignore`(生成物、损坏不可恢复)
  - GBK CSV 转 UTF-8,并把 5 个以 `encoding='gbk'` 读取它的脚本改为 `utf-8`
  - 3 个脚本(`compare_gyroid_power.py`、`compare_gyroid_30w_old_new.py`、`plot_primitive_10w_20w_old.py`)渲染中文标签但未配置中文字体 → 补 SimHei 配置并重生成 4 张 PDF;重生成过程无 "Glyph missing" 警告
  - `plot_primitive_10w_20w_old.py` 引用的数据文件已在 `deprecated/`,修正路径
- 教训:控制台 PowerShell 默认 GBK 会把 UTF-8 中文显示成乱码,判断乱码必须以字节为准;Python 源文件均为合法 UTF-8,并非乱码
- 遗留:`gyroid_10w_20w_*` 两张图基于已被组里废弃(Run 1 无效)的数据,可考虑后续删除
