# 2026-08-16 数据文件重命名

## 工作
- 将全部原始实验记录与派生统计重命名为前缀式 `tpms_<结构><功率>w_<时间戳>`:
  - 16 个原始记录(Gyroid/IWP/Primitive × 10/20/30W + 冷却/升温相)改为 `tpms_gyroid10w_20260808_165138.csv` 形式
  - `deprecated/temperature_record_20260806_162603.csv` → `deprecated/tpms_primitive20w_20260806_162603.csv`
  - 7 个统计文件 `stats_*.csv` / `temperature_stats.csv` 改为 `stats_<结构><功率>w_<日期>[相].csv`
- 两份逐字节相同的 Gyroid 30W 文件(`20260803_171111.csv` 与 `-1.csv`,MD5 一致)按用户要求各自保留并分别改名
- 用脚本按名称长度降序替换全仓库引用(避免短名误匹配长名),更新 79 个 .py/.md/notes 文件
- 未改 `.openscience/provenance.jsonl`/`runs.jsonl`(遥测历史记录,非来源引用)

## 验证
- 65 个 .py 全部通过 `py_compile`;两份 30W 重复文件 MD5 仍一致
- `scripts/plot_temperature_curves.py`、`compare_all_structures.py`、`analyze_iwp_20w.py` 用新名运行成功
- 重跑生成的 3 个 PDF 无内容差异,已 `git checkout` 还原,保持提交纯净
- git 将全部变更识别为 rename(R),103 项暂存

## 推送
- `git push origin main` 成功(`307a2dc..33beeeb`),重命名提交已同步到新远程 `github.com/tsinghuazhou/ATpms-test-data.git`

## 教训
- 批量重命名时先盘点全部引用;用「长名优先替换」避免 `-1`/` (4)` 后缀名被短名误伤
- 运行集成测试后需检查重生成的跟踪产物,内容未变时应还原而非提交
- HTTPS(443)到 GitHub 间歇被重置,但 SSH(22)通常可达;网络波动时直接重试 push 而非过早改路线
